@model ErrorLoggerModel.UserModel

@{
    ViewBag.Title = "EnableUser";
}

<h2 class="title-head">Enable User</h2>

<h3>Are you sure you want to enable this user?</h3>

<div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Enable" class="btn btn-default" style="background: #26A69A; color: #fff;" />
            <button class="btn btn-primary" type="button">
                @Html.ActionLink("Cancel", "ViewUsers")
            </button>
        </div>
    }
</div>
